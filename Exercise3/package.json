{
  "name": "angular-exercises-exercise-2",
  "private": true,
  "version": "0.0.0",
  "description": "Exercise 2",
  "devDependencies": {
    "bower": "^1.3.1",
    "http-server": "^0.6.1"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000"
  }
}
